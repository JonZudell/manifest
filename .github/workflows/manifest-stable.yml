name: Use Manifest Check

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  call-manifest-check:
    uses: ./.github/workflows/manifest-check.yml
    with:
      MANIFEST_IMAGE_TAG: v0.0.6